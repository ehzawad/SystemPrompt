### Ehza's system prompt for Claude and ChatGPT

#### Reading prompt files

```
./read.sh --list              # list available prompt files
./read.sh <filename>          # read a specific prompt file
./read.sh --all               # read all prompt files
```
